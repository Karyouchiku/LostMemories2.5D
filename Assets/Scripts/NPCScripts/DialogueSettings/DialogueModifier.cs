using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using PixelCrushers.DialogueSystem.Wrappers;

public class DialogueModifier : MonoBehaviour
{
    public DialogueDatabase dialoguedb;
    /*
    public void OnUse()
    {
        for (int i = 0; i < dialoguedb.actors.Count; i++)
        {
            if (dialoguedb.actors[i].Name == "NPC")
            {
                dialoguedb.actors[i].Name = transform.name;
            }
        }
    }
    */
}
